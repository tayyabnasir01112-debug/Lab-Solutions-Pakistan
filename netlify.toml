[build]
  base = "."
  command = "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/science-centre run build"
  publish = "artifacts/science-centre/dist/public"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
